const Input = (
  id,
  label,
  type = "text",
  disabled,
  register,
  required,
  //   errors is an object, can look for id of input in errors object for class conditional

  errors,
  error
) => {};
